---
title: "AI"
---

The AI application page tracks phase channels, coil attention, cyclic memory, looped/recursive transformer schedules, and adapter/block indexing. Local compute language is MLX/Mac-first.

The current proved layer is finite indexing, loop-schedule bookkeeping, and loop-exit certificate bookkeeping only. `AIA-B0001` adds an exploratory deterministic phase-channel benchmark fixture for a known-period synthetic task. `AIA-B0002` adds a tiny learned-baseline control: phase lookup wins on a periodic task, while an ordinary scalar threshold wins on a nonperiodic control. `AIA-B0003` adds CPU/optional-MLX backend parity scoring across the current deterministic AI fixtures; if MLX is unavailable, it reports that directly. `AIA-B0004` adds learned-feature baseline controls: cyclic phase features are compared with dense-scalar, learned-position, and wrong-period baselines, plus a nonperiodic scalar-control task. `AIA-B0005` adds harmonic/Fourier feature controls: cyclic phase lookup and correct sine/cosine lookup solve the constructed periodic task, while wrong-frequency, scalar, and learned-position baselines remain controls. `AIM-B0001` adds the same discipline to cyclic memory: a positive slot-lookup fixture, constant/scalar-threshold baselines, collision diagnostics, and a nonperiodic control. `AIM-B0002` adds a coil-retrieval reachability fixture: a selected coil path reaches a constructed long-lag dependency, local and wrong-stride baselines miss it, full attention is the oracle upper bound, and local attention wins the near-lag control. `AIM-B0004` adds content-gated retrieval routing: the gate chooses coil or local candidates for mixed long/near dependencies while static, wrong-gate, union, and full-attention controls report reachability and budget. `AIM-B0010` adds learned content-gate routing: a tiny phase-to-route lookup is fit from examples and compared with static, wrong-period, flipped-gate, union, and full-attention controls. `AIM-B0003` adds a looped-recurrence schedule fixture: adaptive-exit and recurrent-memory controls retain useful intermediate steps, while single-pass, fixed-loop, sparse phase-router, over-looped, and nonperiodic dense-threshold controls keep the claim bounded. `AIM-B0011` adds loop-exit certificate bookkeeping: score trace, exit availability, budget status, and overthinking guardrail status are recorded for a constructed sample. `AIM-B0005` adds token-level recurrence routing: per-token budgets, active-token counts, selected middle block, resolution labels, and wrong/over-loop controls are recorded before any learned router claim. `AIM-B0006` adds a training-free loop-wrapper fixture: a circular phase-budget wrapper is compared with single-pass, fixed-loop, wrong-period, over-loop, and nonperiodic scalar-threshold baselines with CPU/optional-MLX scoring. `AIM-B0007` adds middle-block recurrence routing: selected-block scheduling is compared with full-block, fixed-budget, wrong-block, and over-loop controls while recording block-pass accounting. `AIM-B0008` adds multi-resolution recurrence routing: phase-routed coarse/fine scheduling is compared with single-resolution, fixed-budget, wrong-resolution, and over-loop controls while recording active-sample accounting. `AIM-B0009` adds learned recurrence-schedule controls: a tiny phase-to-budget lookup is fit from examples and compared with fixed-budget, wrong-period, and over-loop controls. `AIRA-B0001` adds the adapter-block version: a positive block-index fixture, ordinary baselines, collision diagnostics, and a nonperiodic control. `AIRA-B0002` adds a MultiCoil/RoPE-style positional fixture: combined-period phase lookup beats single-period, constant, and scalar-threshold baselines on a constructed multi-period task, while the scalar threshold wins the nonperiodic control. `AIRA-B0003` adds a RoPE-style relative phase fixture: correct-period relative lookup recovers constructed query/key lag labels, while wrong-period, query-position, scalar-threshold, and nonperiodic controls remain separated. `AIRA-B0004` adds adapter parameter-budget accounting across dense adapter, LoRA-style low-rank, and block-cyclic shared-table baselines with alias/load diagnostics. `AIRA-B0005` adds circulant mixer validation: circular convolution matches the equivalent dense circulant-matrix baseline while a wrong-shift control and parameter counts keep the claim bounded. These are not model-quality claims.

::: {.learning-unit}

## Case-Study Frame

The circular address spaces are phase channels, memory slots, and adapter blocks. The serious AI question is whether explicit cyclic structure helps when the task actually has periodic structure. The current proved layer only certifies finite indexing behavior.

:::

## AI Program Map

AI is now a dedicated Circle Calculus program, not a side note. The avenues to explore are:

- phase channels and periodic feature routing,
- cyclic memory slots, alias diagnostics, and retrieval fixtures,
- coil/sparse attention for long-context path selection,
- looped and recursive transformer schedules with loop phase, exit certificates, and overthinking guardrails,
- adapter blocks, CoilLinear/circulant mixers, CoilRA, and block-cyclic parameter sharing,
- RoPE, MultiCoil RoPE, winding-aware positions, and torus-valued positional views,
- recurrent, state-space, and convolutional sequence models with explicit period structure,
- harmonic, Fourier, circulant, and NTT-adjacent features,
- quaternion, spherical, Hopf/fiber, and rotation-aware representations, and
- proof-carrying model components whose indexing or schedule behavior can be checked in Lean.

Every avenue needs ordinary baselines, negative controls, and reproducible scripts before it becomes a claim about model quality or speed.

## Looped Recurrence Path

Looped and recursive transformers reuse model blocks across multiple computation passes. The Circle Calculus version should make each pass explicit: loop phase, recurrence budget, state provenance, exit decision, and overthinking boundary. That is a possible interface contribution, not a proved model improvement.

The benchmark lane is `P5-EDGE-010`, seeded by `AIM-B0003`, `AIM-B0011`, `AIM-B0005`, `AIM-B0006`, `AIM-B0007`, `AIM-B0008`, and `AIM-B0009`, with `AIM-B0010` covering the adjacent learned coil/local route-gating story. It should expand toward ordinary dense transformer depth, Universal Transformer recurrence, fixed looped transformers, adaptive early-exit models, recurrent-memory transformers, token-level Mixture-of-Recursions, learned middle-block recurrence, learned multi-resolution recurrence, sparse/MoE looped models, learned attention gates, full/sparse attention baselines, RWKV/Mamba-style recurrent or state-space models, and nonrecursive transformer baselines.

The first lesson goal is to make recurrence readable:

```text
shared block
  -> loop phase
  -> active token set / resolution level
  -> state update
  -> exit or continue
  -> score trace
  -> overthinking boundary
```

The proof boundary stays strict: Lean can certify finite schedule and indexing facts; Python or MLX benchmarks must carry model-quality, runtime, memory, and parameter claims.

`AIM-B0005` turns that lesson into token-level bookkeeping:

```text
token
  -> routed loop budget
  -> selected middle block
  -> active or halted at each loop step
  -> coarse/fine resolution label
  -> wrong-loop and over-loop controls
```

This is the Circle Calculus contribution to the interface: recurrence provenance should be visible before it is sold as reasoning or throughput.

`AIM-B0006` turns the training-free wrapper idea into an auditable fixture:

```text
sample
  -> circular phase budget
  -> run this many loop passes
  -> compare fixed, wrong-period, and over-loop controls
  -> compare nonperiodic scalar-threshold baseline
```

This is useful only as a schedule interface. It is not a claim that a reused block improves a real model.

`AIM-B0007` turns middle-block recurrence into auditable bookkeeping:

```text
selected loop block
  -> required block and budget
  -> full/fixed/wrong/over-loop controls
  -> block-pass accounting
```

This is useful only as a schedule interface. It is not a claim that looping a middle block improves a real model.

`AIM-B0008` turns multi-resolution recurrence into auditable bookkeeping:

```text
sample
  -> required loop budget
  -> required coarse/fine resolution
  -> single-resolution and wrong-resolution controls
  -> active-sample accounting
```

This is useful only as a schedule interface. It is not a claim that alternating compressed and full-resolution passes improves a real model.

`AIM-B0009` turns learned recurrence schedules into auditable bookkeeping:

```text
training sample
  -> phase-to-budget lookup
  -> predicted loop budget
  -> fixed-budget, wrong-period, and over-loop controls
  -> schedule accuracy
```

This is useful only as a learned-schedule harness. It is not evidence that a learned recursive transformer improves quality, speed, memory, context length, perplexity, or reasoning.

## Learning Path

1. Start with phase channels: finite indices, closure, and known-period examples.
2. Compare learned-feature baselines: cyclic features, dense scalar thresholds, learned absolute positions, and wrong-period controls.
3. Compare harmonic/Fourier features: correct sine/cosine phase features, wrong-frequency controls, and scalar controls.
4. Check backend parity: CPU references first, optional MLX only when available.
5. Add cyclic memory: slots, alias diagnostics, and nonperiodic controls.
6. Study coil retrieval: selected long-lag paths, local baselines, wrong-stride baselines, and full-attention oracle behavior.
7. Add content-gated retrieval: coil/local routing, wrong-gate controls, union/full-attention baselines, and candidate-budget accounting.
8. Add learned content-gate retrieval: phase-to-route lookup tables, route accuracy, wrong-period controls, flipped-gate controls, union/full-attention baselines, and candidate-budget accounting.
9. Add looped recurrence: loop phase, exit certificates, score traces, over-loop controls, and recursive transformer baselines.
10. Add token-level recurrence routing: per-token budgets, active-token counts, selected middle block, resolution labels, and wrong-loop controls.
11. Add training-free loop wrappers: circular phase budgets, wrong-period controls, over-loop controls, scalar-threshold controls, and optional MLX scoring.
12. Add middle-block recurrence controls: selected-block schedules, full-block baselines, fixed budgets, wrong-block controls, over-loop controls, and block-pass accounting.
13. Add multi-resolution recurrence controls: coarse/fine phase routing, single-resolution baselines, fixed budgets, wrong-resolution controls, over-loop controls, and active-sample accounting.
14. Add learned recurrence-schedule controls: phase-to-budget lookup tables, fixed-budget baselines, wrong-period controls, over-loop controls, and schedule accuracy.
15. Move to adapters and circulant mixers: block sharing, circular-convolution parity, parameter accounting, and wrong-structure controls.
16. Add MultiCoil positions: combined periods, RoPE-style phase structure, and mismatch controls.
17. Compare RoPE-style relative phase: query/key lag labels, wrong-period controls, query-only controls, and scalar controls.
18. Only then compare learned model baselines and MLX prototypes, with quality, runtime, memory, parameter count, and interpretability reported separately.
19. Treat quaternion, spherical, Hopf/fiber, and `S^7` ideas as later geometry-aware AI lessons, not as shortcuts around the lower-dimensional evidence.

## What To Notice

- Phase-channel proofs are about finite indices, not model quality.
- `AIA-B0003` is backend parity scaffolding, not a speed, quality, or acceleration claim.
- `AIA-B0004` is a learned-feature baseline scaffold, not a neural-network result or real-workload benchmark.
- `AIA-B0005` is a harmonic/Fourier-feature scaffold, not a standard RoPE or language-model benchmark.
- Positive periodic fixtures must be paired with negative controls.
- `AIM-B0001` is a cyclic-memory benchmark harness, not a retrieval or attention theorem.
- `AIM-B0002` is a reachability harness over candidate index sets, not an attention-quality theorem.
- `AIM-B0004` is a gated-route reachability and budget harness, not a learned attention-quality theorem.
- `AIM-B0010` is learned route-table bookkeeping, not evidence about neural learned gates, retrieval quality, context length, runtime, or memory scaling.
- `AIM-B0003` is a looped/recursive transformer schedule harness, not a recursive reasoning, context length, speed, memory, or model-quality result.
- `AIM-B0011` is loop-exit certificate bookkeeping, not evidence about adaptive-exit quality, reasoning, context length, speed, memory, or throughput.
- `AIM-B0005` is token-level recurrence routing bookkeeping, not evidence about learned routers, reasoning, perplexity, throughput, memory, or context length.
- `AIM-B0006` is training-free loop-wrapper bookkeeping, not evidence about learned recurrence, reasoning, language-model quality, throughput, memory, or context length.
- `AIM-B0007` is middle-block recurrence bookkeeping, not evidence about learned middle-block recurrence, reasoning, perplexity, throughput, memory, or context length.
- `AIM-B0008` is multi-resolution recurrence bookkeeping, not evidence about learned compressed/full-resolution routing, reasoning, perplexity, throughput, memory, or context length.
- `AIM-B0009` is learned recurrence-schedule bookkeeping, not evidence about learned recursive transformer quality, reasoning, perplexity, throughput, memory, or context length.
- `AIRA-B0001` is an adapter-block benchmark harness, not a CoilRA, RoPE, parameter-efficiency, or quality theorem.
- `AIRA-B0002` is a MultiCoil/RoPE-style positional harness, not a standard-RoPE comparison or language-model theorem.
- `AIRA-B0003` is a RoPE-style relative phase harness, not an attention-quality, context-length, or language-model theorem.
- `AIRA-B0004` is parameter-count accounting, not evidence about quality, runtime, memory, training stability, or fine-tuning behavior.
- `AIRA-B0005` is circulant-mixer validation and parameter accounting, not evidence about neural-layer quality, runtime, memory, training stability, or hardware efficiency.
- MLX/Mac-first experiments should be reported as benchmarks, not proofs.

::: {.checkpoint-box}

## Checkpoint

Why is the nonperiodic control in `AIA-B0002` important before making any AI-improvement claim?

:::

## Source Trail

<div class="theorem-box" data-theorem-id="AIA-T0001"></div>
<div class="theorem-box" data-theorem-id="AIA-T0004"></div>
<div class="theorem-box" data-theorem-id="AIA-T0005"></div>
<div class="theorem-box" data-theorem-id="AIM-T0001"></div>
<div class="theorem-box" data-theorem-id="AIM-T0004"></div>
<div class="theorem-box" data-theorem-id="AIM-T0005"></div>
<div class="theorem-box" data-theorem-id="AIM-T0006"></div>
<div class="theorem-box" data-theorem-id="AIM-T0007"></div>
<div class="theorem-box" data-theorem-id="AIM-T0008"></div>
<div class="theorem-box" data-theorem-id="AIM-T0009"></div>
<div class="theorem-box" data-theorem-id="AIM-T0010"></div>
<div class="theorem-box" data-theorem-id="AIM-T0011"></div>
<div class="theorem-box" data-theorem-id="AIM-T0012"></div>
<div class="theorem-box" data-theorem-id="AIM-T0013"></div>
<div class="theorem-box" data-theorem-id="AIM-T0014"></div>
<div class="theorem-box" data-theorem-id="AIM-T0015"></div>
<div class="theorem-box" data-theorem-id="AIM-T0016"></div>
<div class="theorem-box" data-theorem-id="AIM-T0017"></div>
<div class="theorem-box" data-theorem-id="AIRA-T0001"></div>
<div class="theorem-box" data-theorem-id="AIRA-T0004"></div>
<div class="theorem-box" data-theorem-id="AIRA-T0005"></div>

<div class="dictionary-box" data-dictionary-id="COMMON-0039"></div>
<div class="dictionary-box" data-dictionary-id="COMMON-0046"></div>
<div class="dictionary-box" data-dictionary-id="COMMON-0047"></div>
<div class="dictionary-box" data-dictionary-id="COMMON-0048"></div>
<div class="dictionary-box" data-dictionary-id="COMMON-0049"></div>
<div class="dictionary-box" data-dictionary-id="COMMON-0050"></div>
<div class="dictionary-box" data-dictionary-id="COMMON-0051"></div>
<div class="dictionary-box" data-dictionary-id="COMMON-0052"></div>
<div class="dictionary-box" data-dictionary-id="COMMON-0053"></div>
<div class="dictionary-box" data-dictionary-id="COMMON-0054"></div>
<div class="dictionary-box" data-dictionary-id="COMMON-0055"></div>
<div class="dictionary-box" data-dictionary-id="COMMON-0056"></div>
<div class="dictionary-box" data-dictionary-id="COMMON-0057"></div>
<div class="dictionary-box" data-dictionary-id="COMMON-0058"></div>
<div class="dictionary-box" data-dictionary-id="COMMON-0059"></div>
<div class="dictionary-box" data-dictionary-id="COMMON-0067"></div>
<div class="dictionary-box" data-dictionary-id="COMMON-0068"></div>
<div class="dictionary-box" data-dictionary-id="COMMON-0069"></div>
<div class="dictionary-box" data-dictionary-id="COMMON-0070"></div>
<div class="dictionary-box" data-dictionary-id="COMMON-0071"></div>

<p data-paper-ref="papers/applications/PAPER_AI_01_CIRCLE_AI_ARCHITECTURES.md">AI architecture paper: <a href="https://github.com/corbensorenson/circle-calculus/blob/main/papers/applications/PAPER_AI_01_CIRCLE_AI_ARCHITECTURES.md">Circle AI Architectures</a></p>
<p data-paper-ref="papers/applications/PAPER_AI_02_COIL_ATTENTION_AND_MEMORY.md">Memory paper: <a href="https://github.com/corbensorenson/circle-calculus/blob/main/papers/applications/PAPER_AI_02_COIL_ATTENTION_AND_MEMORY.md">Coil Attention And Memory</a></p>
<p data-paper-ref="papers/applications/PAPER_AI_03_COILRA_AND_MULTICOIL_ROPE.md">Adapter paper: <a href="https://github.com/corbensorenson/circle-calculus/blob/main/papers/applications/PAPER_AI_03_COILRA_AND_MULTICOIL_ROPE.md">CoilRA And MultiCoil RoPE</a></p>

::: {.warning-box}
Python and optional MLX benchmark fixtures are executable references only. They do not replace Lean proofs and do not prove that circles improve neural networks.
:::

<script type="module" src="../../widgets/shared/widget_base.js"></script>
